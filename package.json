{
  "name": "@local/openclaw-harness-core",
  "version": "1.0.0",
  "type": "module",
  "description": "Filesystem-first OpenClaw harness runtime",
  "license": "UNLICENSED",
  "peerDependencies": {
    "openclaw": ">=2026.3.13"
  },
  "scripts": {
    "check": "node --check index.js && node --test tests/*.test.js",
    "test": "node --test tests/*.test.js"
  },
  "openclaw": {
    "extensions": [
      "./index.js"
    ]
  }
}

